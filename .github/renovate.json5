{
  $schema: 'https://docs.renovatebot.com/renovate-schema.json',
  enabled: true,
  extends: [
    'config:best-practices',
  ],
  prHourlyLimit: 0,
  schedule: [
    'after 6am and before 4pm on sunday',
  ],
  timezone: 'Asia/Jerusalem',
  platformAutomerge: true,
  prConcurrentLimit: 10,
  ignoreDeps: [
    'lscr.io/linuxserver/jellyfin',
  ],
  packageRules: [
    {
      description: 'Disable auto-merge for postgres major updates',
      matchPackageNames: ['postgres'],
      matchManagers: ['docker-compose'],
      matchUpdateTypes: ['major'],
      automerge: false,
      platformAutomerge: false,
    },
    {
      description: 'Use docker versioning for all Docker Compose images: it handles the v-prefix, zero-padded calver months (pihole 2026.04.0), postgres major versions, and treats variant suffixes like trawl -baseline as incompatible.',
      matchManagers: [
        'docker-compose',
      ],
      matchDatasources: [
        'docker',
      ],
      versioning: 'docker',
    },
    {
      description: 'Docker Compose major image updates',
      matchManagers: [
        'docker-compose',
      ],
      matchDatasources: [
        'docker',
      ],
      commitMessagePrefix: 'deps(compose):',
      matchUpdateTypes: [
        'major',
      ],
      minimumReleaseAge: '21 days',
      automerge: false,
      platformAutomerge: false,
    },
    {
      description: 'Auto-merge Docker Compose minor updates',
      matchManagers: [
        'docker-compose',
      ],
      matchDatasources: [
        'docker',
      ],
      commitMessagePrefix: 'deps(compose):',
      matchUpdateTypes: [
        'minor',
      ],
      minimumReleaseAge: '7 days',
      automerge: true,
      automergeType: 'pr',
      automergeStrategy: 'squash',
    },
    {
      description: 'Auto-merge Docker Compose patch updates',
      matchManagers: [
        'docker-compose',
      ],
      matchDatasources: [
        'docker',
      ],
      commitMessagePrefix: 'deps(compose):',
      matchUpdateTypes: [
        'patch',
      ],
      automerge: true,
      automergeType: 'pr',
      automergeStrategy: 'squash',
    },
    {
      matchManagers: [
        'docker-compose',
      ],
      matchDatasources: [
        'docker',
      ],
      matchCurrentVersion: 'latest',
      pin: {
        enabled: true,
      },
    },
  ],
  ignoreTests: true,
}
